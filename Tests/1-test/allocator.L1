(((ebx <- 11)
  (eax <- (allocate ebx 0))
  (ecx <- eax)
  ((mem ecx 4) <- 3)
  ((mem ecx 8) <- 5)
  ((mem ecx 12) <- 7)
  ((mem ecx 16) <- 9)
  ((mem ecx 20) <- 11)
  (eax <- (mem ecx 16))
  (eax <- (print eax))))
