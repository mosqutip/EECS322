((print (:main))
 (:main
  ()
  (if (let ((x 47))
        (let ((y (+ x 1))) (let ((x (+ y 1))) (let ((y 11)) (+ x y)))))
    (new-tuple 5 6 7)
    (new-tuple 8 9 10)))
 (:fn (x y z) y)
 (:gn (x y) y)
 (:hn (x) (aref x 0))
 (:in () 42))
