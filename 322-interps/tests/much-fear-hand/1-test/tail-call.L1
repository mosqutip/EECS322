(((eax <- 10)
  (ebx <- 20)
  (eax += ebx)
  (call :foo))
 (:foo
  (ebx += ebx)
  (ebx += 1)
  (eax <- (print ebx))
  (ecx <- ebx)
  (call :foo1)
  (return))
 (:foo1
  (ecx += ecx)
  (ecx += 1)
  (eax <- (print ecx))
  (return)))
