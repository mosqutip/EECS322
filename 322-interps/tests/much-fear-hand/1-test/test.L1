(((eax <- (allocate 31 21))
  (esi <- eax)
  (ebx <- (mem eax 4))
  (ecx <- eax)
  (eax <- (print ebx))
  (ebx += ebx)
  (ebx += 1)
  ((mem esi 8) <- ebx)
  (edx <- (mem esi 8))
  (eax <- (print edx))))
