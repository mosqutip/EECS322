(((ebx <- 10)
  (esi <- 20)
  (eax <- ebx)
  (eax += esi)
  (call :foo))
 (:foo
  (esp -= 4)
  ((mem ebp -4) <- edi)
  (eax += eax)
  (edi <- eax)
  (edi += 1)
  (eax <- (print edi))
  (edi <- (mem ebp -4))
  (return)))
