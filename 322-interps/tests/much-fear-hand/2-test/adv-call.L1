(((eax <- 10)
  (ebx <- 20)
  (eax += ebx)
  (call :foo)
  (eax *= 2)
  (eax += 1)
  (eax <- (print eax)))
 (:foo
  (ebx <- eax)
  (eax += ebx)
  (eax += 1)
  (eax <- (print eax))
  (eax <- ebx)
  (return)))
