(((call :main))
 (:main
  (eax <- (allocate 11 1)) 
  (a <- eax)
  (b <- :f)
  (eax <- a) 
  (tail-call b))
 (:f (array <- eax) 
  (length <- (mem array 0))
  (length <<= 1)
  (length += 1)
  (eax <- (print length))
  (eax <- 1) 
  (return)))
