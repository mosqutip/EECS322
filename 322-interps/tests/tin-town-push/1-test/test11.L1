(((eax <- (allocate 3 1))
  (edi <- eax)
  (edi += 4)
  (ecx <- 5)
  :loop
  ((mem edi 0) <- ecx)
  (eax <- ecx)
  (eax *= 2)
  (eax += 1)
  (eax <- (print eax))
  (ecx <- (mem edi 0))
  (ecx -= 1)
  (cjump ecx < 0 :end :loop)
  :end))
