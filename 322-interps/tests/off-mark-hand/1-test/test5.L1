((
  (eax <- 10)
  (edx <- -10)
  (eax <- eax < edx)
  (eax <<= 1)
  (eax += 1)
  (eax <- (print eax))
  (eax <- 10)
  (edx <- 10)
  (eax <- eax < edx)
  (eax <<= 1)
  (eax += 1)
  (eax <- (print eax))
  (eax <- 10)
  (edx <- 12)
  (eax <- eax < edx)
  (eax <<= 1)
  (eax += 1)
  (eax <- (print eax))
 ))
