(
  (eax <- 7)
  (eax += 8)
  (eax -= 1)
  (eax >>= 1)
  ((mem ebp -100) <- 1000)
  (spill_0 <- (mem ebp -100))
  (eax *= spill_0)
  (y <- eax)
  ((mem ebp -100) <- y)
  (eax <<= 1)
  (eax += 1)
  (eax <- (print eax))
)