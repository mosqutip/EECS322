(((ebx <- 10)
  (ebx <<= 2)
  (ebx += ebx)
  (ebx += 1)
  (eax <- (print ebx))
  (ebx <- 48)
  (ebx >>= 3)
  (ebx += ebx)
  (ebx += 1)
  (eax <- (print ebx))
  (ebx <- -10)
  (ebx <<= 2)
  (ebx += ebx)
  (ebx += 1)
  (eax <- (print ebx))
  (ebx <- -48)
  (ebx >>= 3)
  (ebx += ebx)
  (ebx += 1)
  (eax <- (print ebx))))