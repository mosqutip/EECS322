(((s <- 5)
  (t <- 3)
  (s *= t)
  (eax <- s)
  (eax += t)
  (eax += 1)
  (call :printeax)
  (call :printeaxplustwo)
  (eax <- (print s)))
 (:printeax
  (s <- eax)
  (eax <- (print s))
  (eax <- s)
  (return))
 (:printeaxplustwo
  (s <- eax)
  (s += 2)
  (eax <- (print s))
  (eax <- s)
  (eax -= 2)
  (return)))
