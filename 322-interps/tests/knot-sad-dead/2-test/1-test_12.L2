(((eax <- 14)
  (call :convertForPrint)
  (eax <- (print eax))
  (eax <- 15)
  (call :convertForPrint)
  (eax <- (print eax))
  (eax <- 0)
  (call :convertForPrint)
  (eax <- (print eax))
  (eax <- -7)
  (call :convertForPrint)
  (eax <- (print eax))
  (eax <- -16)
  (call :convertForPrint)
  (eax <- (print eax)))
 (:convertForPrint
  (eax += eax)
  (eax += 1)
  (return)))