(((eax <- 5)
  (edi <- 6)
  (eax += edi)
  (eax += eax)
  (eax += 1)
  (eax <- (print eax))
  
  (ebx <- 8)
  (ecx <- 9)
  (ebx *= ecx)
  (ebx += ebx)
  (ebx += 1)
  (eax <- (print ebx))
  
  (edx <- 7)
  (edi <- 12)
  (edx &= edi)
  (edx += edx)
  (edx += 1)
  (eax <- (print edx))))