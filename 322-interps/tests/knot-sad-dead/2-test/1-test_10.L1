(((eax <- 5)
  (eax <- eax = 5)
  (call :print)
  (eax <- 5)
  (eax <- eax <= 5)
  (call :print)
  (eax <- 5)
  (eax <- eax < 5)
  (call :print)
  
  (eax <- 5 = 5)
  (call :print)
  (eax <- 5 <= 5)
  (call :print)
  (eax <- 5 < 5)
  (call :print)
  
  (eax <- 5 = 6)
  (call :print)
  (eax <- 5 <= 6)
  (call :print)
  (eax <- 5 < 6)
  (call :print)
  
  (eax <- 5 = -5)
  (call :print)
  (eax <- 5 <= -5)
  (call :print)
  (eax <- 5 < -5)
  (call :print)
  
  (eax <- -6 = 5)
  (call :print)
  (eax <- -6 <= 5)
  (call :print)
  (eax <- -6 < 5)
  (call :print))
 (:print
  (eax += eax)
  (eax += 1)
  (eax <- (print eax))
  (return)))