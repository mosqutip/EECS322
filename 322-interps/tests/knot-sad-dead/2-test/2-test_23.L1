(((eax <- 5)
  (ecx <- 2)
  (eax <<= ecx)
  (eax += eax)
  (eax += 1)
  (eax <- (print eax))
  (ecx <- 2)
  (ecx <<= ecx)
  (ecx += ecx)
  (ecx += 1)
  (eax <- (print ecx))
  (ecx <- -4)
  (ecx >>= 10)
  (ecx += ecx)
  (ecx += 1)
  (eax <- (print ecx))
  (ecx <- 4)
  (ecx >>= ecx)
  (ecx += ecx)
  (ecx += 1)
  (eax <- (print ecx))))