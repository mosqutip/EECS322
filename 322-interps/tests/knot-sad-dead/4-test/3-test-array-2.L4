((let ([arr 5])
   (let ([arr (new-tuple 1 2 arr 4)])
     (let ([trash (print arr)])
       (let ([x 2])
         (let ([x (aref arr x)])
           (let ([trash (print x)])
             (let ([x 1])
               (let ([arr (aref arr x)])
                 (let ([trash (print arr)])
                   (let ([arr (new-tuple 1 2 3 4)])
                     (let ([x 1])
                       (let ([y 5])
                         (let ([x (aset arr x y)])
                           (let ([trash (print arr)])
                             (let ([arr (new-tuple 1 2 3 4)])
                               (let ([x 1])
                                 (let ([y 5])
                                   (let ([y (aset arr x y)])
                                     (let ([trash (print arr)])
                                       (let ([arr (new-tuple 1 2 3 4)])
                                         (let ([x 1])
                                           (let ([y 5])
                                             (let ([arr2 arr])
                                               (let ([arr (aset arr x y)])
                                                 (let ([trash (print arr2)])
                                                   (print arr)))))))))))))))))))))))))))
