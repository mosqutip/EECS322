((let ([arr (new-array 5 3)])
   (let ([arr (print arr)])
     (let ([arr (new-tuple 0 1 2 4 5 arr)])
       (let ([x (print arr)])
         (let ([x (aref arr 3)])
           (let ([x (print x)])
             (let ([x 3])
               (let ([x (aref arr x)])
                 (let ([x (print x)])
                   (let ([x (alen arr)])
                     (let ([x (print x)])
                       (let ([x (aset arr 0 1)])
                         (let ([x 2])
                           (let ([y (aset arr 1 x)])
                             (let ([y (aset arr x 3)])
                               (let ([x 3])
                                 (let ([y (aset arr x x)])
                                   (let ([y 4])
                                     (let ([y (aset arr y x)])
                                       (let ([z (print arr)])
                                         (aset arr 4 0))))))))))))))))))))))
