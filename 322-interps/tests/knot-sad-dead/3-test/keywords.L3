((let ([mem 0])
   (let ([goto 1])
     (let ([cjump 2])
       (let ([call 3])
         (let ([tail-call 4])
           (let ([return 5])
             (let ([allocate 6])
               (let ([array-error 7])
                 (let ([arr (new-tuple mem goto cjump call tail-call return allocate array-error)])
                   (print arr)))))))))))
