((let ([a (number? 4)])
   (let ([b (number? a)])
     (let ([c (a? 4)])
       (let ([d (a? a)])
         (let ([arr (new-tuple a b c d)])
             (let ([a (number? arr)])
                 (let ([b (a? arr)])
                   (let ([arr (new-tuple a b arr)])
                     (print arr))))))))))
