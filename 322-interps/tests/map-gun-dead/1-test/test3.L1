((	
	(esi <- 9)
        (eax <- (allocate esi 3))
	(edi <- eax)
	(ebx <- 5)
	(edx <- ebx)
	(edx -= 4)
	(edx -= 1)
	(edx *= 2)
	(edx += edi)
	(edx += 4)
	((mem edx 0) <- 5)
	((mem edx 4) <- 7)
	(ebx <- (mem edx 4))
	(edx <- (mem edx 0))
	(eax <- (print edx))
	(eax <- (print ebx))

))
