((
	(esi <- 21)
	(eax <- (allocate esi 3))
	(edi <- eax)
	(ebx <- eax)
	(edx <- 40)
	(edx += edi)
	(edx <- (mem edx 0))
	(eax <- (print edx))

	(esi <- 21)
	(eax <- (allocate esi 5))
	(edi <- eax)
	(edx <- 40)
	(edx += edi)
	(edx <- (mem edx 0))
	(eax <- (print edx))

	(esi <- 1)
	(eax <- (allocate esi 1))
	
	(esi <- 21)
	(eax <- (allocate esi 7))
	(edi <- eax)
	(edx <- 40)
	(edx += edi)
	(edx <- (mem edx 0))
	(eax <- (print edx))

	(edx <- 40)
	(edx += ebx)
	(edx <- (mem edx -8))
	(eax <- (print edx))
))
