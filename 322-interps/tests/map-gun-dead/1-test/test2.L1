((	
	(esi <- 9)
        (eax <- (allocate esi 3))
	(edi <- eax)
	(ebx <- 5)
	(edx <- ebx)
	(edx -= 4)
	(edx -= 1)
	(edx *= 2)
	(edx += edi)
	(edx += 4)
	(edx <- (mem edx 0))
	(eax <- (print edx))

))
	
