((let ([r1 (:funcb 3)])
   (let ([r2 (:funcc 4)])
     (let ([result (:funca r1 r2)])
       (print result))))
 (:funca (x y)
         (+ x y))
 (:funcb (z)
         (* z 2))
 (:funcc (a)
         (* a a)))