(((ebx <- 10)
  (ebx >>= 1)
  (eax <- (print ebx))
  (ebx <<= 1)
  (ebx += 1)
  (eax <- (print ebx))
  (ebx >>= 0)
  (eax <- (print ebx))
  (ebx <<= 0)
  (eax <- (print ebx))
  (ecx <- 2)
  (ebx <<= ecx)
  (ebx += 1)
  (eax <- (print ebx))
  (ecx <- 2)
  (ebx >>= ecx)
  (eax <- (print ebx))))
