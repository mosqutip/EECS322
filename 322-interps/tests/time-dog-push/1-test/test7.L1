(((eax <- (allocate 11 5))
  (eax <- (print eax))
  (eax <- (allocate 101 5))
  (eax <- (print eax))
  (eax <- (allocate 1001 5))
  (call :print_size)
  (eax <- (allocate 10001 5))
  (call :print_size)
  (ebx <- 10001)
  (ecx <- 9)
  (eax <- (allocate ebx ecx))
  (call :print_size))
 (:print_size
  (eax <- (mem eax 0))
  (eax <<= 1)
  (eax += 1)
  (eax <- (print eax))
  (return))) 
