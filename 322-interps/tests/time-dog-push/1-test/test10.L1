(((ebx <- 5)
  (esi <- 5)
  (ebx += 4)
  (esi += 4)
  (eax <- (print ebx))
  (eax <- (print esi))
  (ebx += 0)
  (esi += 0)
  (eax <- (print ebx))
  (eax <- (print esi))
  (ebx -= 4)
  (esi -= 4)
  (eax <- (print ebx))
  (eax <- (print esi))
  (ebx -= 0)
  (esi -= 0)
  (eax <- (print ebx))
  (eax <- (print esi))
  (ebx *= 3)
  (esi *= 3)
  (eax <- (print ebx))
  (eax <- (print esi))
  (ebx *= 0)
  (esi *= 0)
  (ebx += 1)
  (esi += 1)
  (eax <- (print ebx))
  (eax <- (print esi))
  (ebx <- 5)
  (esi <- 5)
  (ebx &= 1)
  (esi &= 1)
  (eax <- (print ebx))
  (eax <- (print esi))))
