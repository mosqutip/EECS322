(((esp += -12)
  ((mem ebp -12) <- 0)
  ((mem ebp -8) <- 0)
  ((mem ebp -4) <- 0)
  (eax <- (allocate 15 333))
  (edi <- 1)
  (edx <- 3)
  ((mem ebp -12) <- 5)
  ((mem ebp -8) <- 7)
  ((mem ebp -4) <- 9)
  (ebx <- 11)
  (esi <- 13)
  (eax <- eax)
  (ecx <- eax)
  (eax += 4)
  ((mem eax 0) <- edi)
  (eax += 4)
  ((mem eax 0) <- edx)
  (eax += 4)
  (edi <- (mem ebp -12))
  ((mem eax 0) <- edi)
  (eax += 4)
  (edi <- (mem ebp -8))
  ((mem eax 0) <- edi)
  (eax += 4)
  (edi <- (mem ebp -4))
  ((mem eax 0) <- edi)
  (eax += 4)
  ((mem eax 0) <- ebx)
  (eax += 4)
  ((mem eax 0) <- esi)
  (eax <- (print ecx))
  (esp -= -12)))
