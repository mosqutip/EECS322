(((eax <- 1)
  (eax <- (allocate 21 21))
  (eax <- (mem eax 4))
  (eax <<= 3)
  (eax >>= 4)
  (ecx <- eax)
  (eax >>= ecx)
  (eax <- eax < ecx)
  (eax &= 1)
  (eax += 4)
  (eax -= 2)
  (eax *= 2)
  (call :label)
  (eax <- (print eax)))
 (:label
  (eax += eax)
  (eax += 1)
  (return)))
