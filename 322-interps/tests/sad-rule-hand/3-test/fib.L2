(((eax <- 10)
  (call :fib)
  (eax += eax)
  (eax += 1)
  (eax <- (print eax)))
 (:fib
  (cjump eax < 2 :base :recur)
  :base
  (eax <- 1)
  (return)
  :recur
  (arg <- eax)
  (arg -= 1)
  (eax <- arg)
  (call :fib)
  (result <- eax)
  (arg -= 1)
  (eax <- arg)
  (call :fib)
  (eax += result)
  (return)))
